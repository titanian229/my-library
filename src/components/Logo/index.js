import React from 'react'

const Logo = () => {
    return (
        <span style={{fontWeight: "bold", color: "white", marginRight: "auto", float: "left"}}>
            MyLibrary
        </span>
    )
}

export default Logo;

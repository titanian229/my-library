import React from 'react'
import {Typography } from 'antd'
const { Title } = Typography
const Saved = () => {
    return (
        <div>
            <Title>Saved</Title>
        </div>
    )
}

export default Saved;

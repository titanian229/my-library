import React from 'react'
import {Typography } from 'antd'
const { Title } = Typography
const Search = () => {
    return (
        <div>
            <Title>Search</Title>
        </div>
    )
}

export default Search;
